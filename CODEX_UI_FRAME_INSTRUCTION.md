# Codex向け Katakana RPG Ver.1.3 UI枠導入実装指示書

## 目的
添付のUI画像アセットを、既存Katakana RPG Ver.1.3へ組み込んでください。
今回の作業はUI装飾の追加のみです。ゲームルール、教育仕様、画面遷移、HP、問題数、ヒント仕様、学習履歴は変更しないでください。

## 使用アセット
```text
assets/images/ui/
├── ui-frame-panel-border-blue.png
├── ui-frame-panel-glass-blue.png
├── ui-frame-panel-cream.png
├── ui-frame-pill-blue.png
└── ui-frame-pill-green.png
```

### 推奨の主力
`ui-frame-panel-border-blue.png`

主パネルでは、この透明枠の内側にCSSで半透明白を敷いてください。
`ui-frame-panel-glass-blue.png` は比較確認用の便利素材であり、調整しやすさの観点ではCSS方式を優先してください。

## 共通原則
- 背景画像を活かしつつ、文字の可読性を最優先する。
- 主パネルの内側は `rgba(255,255,255,0.75～0.82)` を目安とする。
- Chromebook + Chromeを主対象とする。
- UI枠導入による縦スクロール・横スクロールを発生させない。
- 枠画像内へ文字を焼き込まない。文字はHTMLテキストのままにする。
- 枠画像は装飾のみで、クリックイベントを持たせない。
- A01～A16の既存受入テストを壊さない。

## 1. タイトル画面
- 背景、EGEロゴ、ゲームタイトル構造は既存のビジュアル実装を維持する。
- ゲームタイトル／説明／開始操作をまとめる中央パネルに `ui-frame-panel-border-blue.png` を使用してよい。
- 内側は半透明白。
- 「はじめる」の大ボタン、見出し帯には `ui-frame-pill-blue.png` を使用してよい。
- EGEロゴより「カタカナRPG」がゲーム名として認識できる優先順位を維持する。

## 2. バトル画面
### 上部
- モード名／問題番号などの短いラベルには `ui-frame-pill-blue.png`。
- Normalの補助ラベル等に色分けが必要な場合のみ `ui-frame-pill-green.png`。
- HP表示は既存の見やすさを優先し、無理に画像枠へ置き換えなくてよい。

### 問題パネル
- 最優先で `ui-frame-panel-border-blue.png` + 半透明白CSSを使用。
- 問題文字はパネル中央。
- 背景は薄く透けるが、カタカナは常に明瞭に読めること。
- Demon King背景では不透明度を高めてもよい。

### 回答パネル
- 問題パネルより装飾を弱くする。
- `ui-frame-panel-border-blue.png` または既存のシンプルな白パネルを使用。
- 文字選択操作のクリック領域を狭くしない。

### ヒント
- 絵ヒントは小型のパネルに載せる。
- 絵＋正解単語を表示する場合も主画面のレイアウトを大きく移動させない。

## 3. リザルト／復習
- `bg-stage-result.webp` は維持する。
- 結果情報、復習単語表示に `ui-frame-panel-border-blue.png` + 半透明白を使用。
- 「もういちど」「つぎへ」等の前向き操作には `ui-frame-pill-green.png` を使用してよい。
- 「タイトルへ」は `ui-frame-pill-blue.png` を使用してよい。
- 敗北後も暗い・否定的なUIへ変更しない。

## 4. 「やめる」確認モーダル
- 中央に小さめの `ui-frame-panel-border-blue.png` + 半透明白。
- モーダル表示中は既存仕様どおり背面入力を停止。
- 「つづける」「タイトルにもどる」の意味が色だけに依存しないよう文字で明示。

## 5. レスポンシブ
1366×768相当以下の実ビューポートで確認する。
小さい画面では枠自体を縮小し、内部の文字サイズと操作領域を優先する。
画像の縦横比が極端に崩れる場合は、同一素材を無理に全用途へ使わず既存CSSパネルを残す。

## 6. 実装時のCSS
同梱 `ui-frame-reference.css` を参考にしてよい。ただし既存CSS構成へ合わせて統合すること。
そのまま丸ごと追加する必要はない。

## 7. 完了確認
既存A01～A16を再実行し、さらに以下を確認する。

- U01 タイトル中央パネルが背景上で読みやすい
- U02 問題文字が全モードで明確に読める
- U03 回答操作領域が狭くなっていない
- U04 ヒント画像と正解単語が読みやすい
- U05 リザルト／復習画面が読みやすい
- U06 モーダル中の背面入力が無効
- U07 Chromebook 1366×768相当以下で不要スクロールなし
- U08 UI画像読み込み失敗時もゲームロジックは停止しない

## 完了報告
1. 変更ファイル
2. 使用したUI枠と画面の対応
3. CSS変更内容
4. A01～A16結果
5. U01～U08結果
6. 既知の表示上の問題
を報告してください。
